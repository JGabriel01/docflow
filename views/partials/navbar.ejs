<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/processos">
      <i class="bi bi-folder-check text-primary fs-4"></i>
      <span>DocFlow</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarContent">
      <% if (typeof empresa !== 'undefined' && empresa) { %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/processos">
              <i class="bi bi-kanban me-1"></i>Processos
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/clientes">
              <i class="bi bi-people me-1"></i>Clientes
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/empresas/perfil">
              <i class="bi bi-building me-1"></i>Minha Empresa
            </a>
          </li>
        </ul>
        <div class="d-flex align-items-center gap-3">
          <% if (empresa.plano === 'PRO') { %>
            <span class="badge badge-pro px-2.5 py-1.5 rounded-pill shadow-sm">
              <i class="bi bi-star-fill me-1"></i>PLANO PRO
            </span>
          <% } else { %>
            <span class="badge bg-secondary px-2.5 py-1.5 rounded-pill">
              PLANO GRÁTIS
            </span>
          <% } %>
          <div class="text-light small text-truncate" style="max-width: 180px;" title="<%= empresa.razaoSocial %>">
            <%= empresa.razaoSocial %>
          </div>
          <form action="/logout" method="POST" class="d-inline m-0">
            <button type="submit" class="btn btn-outline-light btn-sm d-flex align-items-center gap-1">
              <i class="bi bi-box-arrow-right"></i>
              <span>Sair</span>
            </button>
          </form>
        </div>
      <% } else { %>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/login">Entrar</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-primary btn-sm ms-lg-2" href="/empresas/cadastro">Cadastrar Empresa</a>
          </li>
        </ul>
      <% } %>
    </div>
  </div>
</nav>
